## Task Statement

Add browser-level E2E coverage for the core respondent journey in Good Morning,
Nikhil.

## Desired Outcome

Introduce a real browser test that proves the primary happy-path episode flow
works from consent gate to final completion, and wire the repo so this journey
can be run repeatedly.

## Known Facts / Evidence

- The app is a single-screen-orchestrated survey flow in `src/app/page.tsx`.
- The current automated coverage in `test/` is unit/integration-only; no
  Playwright/browser journey exists yet.
- The current behavioral source of truth is `docs/user-journeys.md`.
- In local test environments with no Supabase env vars, `HAS_SUPABASE` is false,
  so the survey can run entirely client-side.
- The survey already includes:
  - media consent gate
  - visible question prompts
  - review screen
  - final submit completion state
- `TwoText` still has no explicit skip path, so a happy-path journey must answer it.

## Constraints

- Repo-specific workflow requires a quick-plan lane before edits.
- Keep changes small, reviewable, and reversible.
- New dependency addition is acceptable only insofar as it is required to add
  real E2E coverage for the requested task.
- Verification must include tests, lint, typecheck, and architect review.

## Unknowns / Open Questions

- Whether existing audio behavior in real browsers will require stubbing or can
  be handled by the current consent/skip controls alone.
- Whether `next dev` or `next start` is the more stable Playwright target for
  this repo.

## Likely Codebase Touchpoints

- `package.json`
- new `playwright.config.ts`
- new `e2e/` or `test/e2e/` spec file
- possibly small testability hooks only if the browser journey cannot be made
  reliable from current UI alone
- `.planning/STATE.md`

