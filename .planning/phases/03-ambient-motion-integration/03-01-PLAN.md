---
phase: 03-ambient-motion-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/scene-transition.test.tsx
  - src/components/SceneTransition.tsx
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-05

must_haves:
  truths:
    - "SceneTransition fires channel static overlay when previousShow differs from screen.show"
    - "SceneTransition does NOT fire static overlay when previousShow matches screen.show (within-show transition)"
    - "SceneTransition uses dissolve for SAME_SET_PAIRS transitions"
    - "All behaviors are proven by automated tests"
  artifacts:
    - path: "test/scene-transition.test.tsx"
      provides: "SceneTransition unit tests covering show-change, within-show, dissolve, and first-screen cases"
      min_lines: 50
  key_links:
    - from: "test/scene-transition.test.tsx"
      to: "src/components/SceneTransition.tsx"
      via: "import { SceneTransition }"
      pattern: "import.*SceneTransition"
---

<objective>
Add automated tests for SceneTransition proving VIS-01 (static fires on show changes) and VIS-02 (static does NOT fire on within-show transitions like 3A->3B->3C).

Purpose: VIS-02 is the only non-Complete requirement in Phase 3. The logic in SceneTransition.tsx already handles it correctly (isShowChange checks previousShow !== screen.show), but there are zero tests. This plan adds the test suite that proves the behavior and closes VIS-02.

Output: test/scene-transition.test.tsx with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/SceneTransition.tsx
@src/data/screens.ts
@test/ambient-map.test.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/data/screens.ts:
```typescript
export interface Screen {
  id: string;
  show: string;
  showEmoji: string;
  audio: string;
  bg: string;
  captions: string[];
  ui: UIType;
  uiConfig?: Record<string, unknown>;
  duration?: number;
  uiRevealAt?: number;
  video?: string;
}
```

From src/components/SceneTransition.tsx:
```typescript
interface SceneTransitionProps {
  screen: Screen;
  previousShow?: string;
  children: React.ReactNode;
}

// Key logic:
// isShowChange = previousShow !== undefined && previousShow !== screen.show
// useDissolve = previousShow !== undefined && SAME_SET_PAIRS.has(transitionKey(previousShow, screen.show))
// SAME_SET_PAIRS = new Set(["Control Room→Credits"])
```

Show sequence from screens.ts (for within-show detection):
- Cold Open, Welcome, Meet Our Audience
- Family Feud, Family Feud, Family Feud (3A->3B->3C = within-show)
- Commercial Break, Commercial Break (4A->4B = within-show)
- The Bachelor, The Bachelor, The Bachelor (5A->5B->5C = within-show)
- Shark Tank, Shark Tank (6A->6B = within-show)
- Survivor, Maury, Control Room, Credits
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SceneTransition test suite proving VIS-01 and VIS-02</name>
  <files>test/scene-transition.test.tsx</files>
  <read_first>
    - src/components/SceneTransition.tsx
    - src/data/screens.ts
    - test/ambient-map.test.ts (for project test conventions)
  </read_first>
  <behavior>
    - Test 1 (VIS-01): When previousShow="Family Feud" and screen.show="Commercial Break", a div with backgroundImage containing "tv-static.png" renders (show-change fires static)
    - Test 2 (VIS-02): When previousShow="Family Feud" and screen.show="Family Feud", NO div with backgroundImage containing "tv-static.png" renders (within-show = no static)
    - Test 3 (VIS-02): When previousShow="Commercial Break" and screen.show="Commercial Break", NO static div renders (within-show sponsor screens)
    - Test 4 (VIS-02): When previousShow="The Bachelor" and screen.show="The Bachelor", NO static div renders (within-show bachelor screens)
    - Test 5: When previousShow is undefined (first screen), NO static div renders
    - Test 6: When previousShow="Control Room" and screen.show="Credits", a dissolve div (bg-black, no tv-static) renders instead of static (SAME_SET_PAIRS)
  </behavior>
  <action>
Create test/scene-transition.test.tsx using vitest + @testing-library/react (already in the project per the existing test files).

Import SceneTransition from @/components/SceneTransition. Create a minimal mock Screen object factory:

```typescript
function makeScreen(show: string): Screen {
  return {
    id: `test-${show.toLowerCase().replace(/\s+/g, "-")}`,
    show,
    showEmoji: "🎬",
    audio: "/vo/test.mp3",
    bg: "/sets/test.webp",
    captions: ["Test caption"],
    ui: "none" as UIType,
  };
}
```

For each test, render `<SceneTransition screen={makeScreen(show)} previousShow={previousShow}><div data-testid="child" /></SceneTransition>` and query the container for the static overlay div.

Detection strategy:
- Static overlay: query for element with style containing "tv-static.png" using container.querySelector('[style*="tv-static"]')
- Dissolve overlay: query for element with class containing "bg-black" that is NOT the child
- Child renders: screen.getByTestId("child") confirms children always render

Write 6 tests in a describe("SceneTransition") block:
1. "fires channel static on show-segment change (VIS-01)" - previousShow="Family Feud", show="Commercial Break" -> static div exists
2. "does NOT fire static on within-show transition: Family Feud (VIS-02)" - previousShow="Family Feud", show="Family Feud" -> no static div
3. "does NOT fire static on within-show transition: Commercial Break (VIS-02)" - previousShow="Commercial Break", show="Commercial Break" -> no static div
4. "does NOT fire static on within-show transition: The Bachelor (VIS-02)" - previousShow="The Bachelor", show="The Bachelor" -> no static div
5. "does NOT fire static on first screen (no previousShow)" - previousShow=undefined -> no static div
6. "fires dissolve (not static) for Control Room to Credits" - previousShow="Control Room", show="Credits" -> no static div, has dissolve div

Note: framer-motion's motion.div renders as a regular div with inline styles. The static overlay uses `style={{ backgroundImage: "url(/textures/tv-static.png)" }}` so querying `[style*="tv-static"]` will find it. The dissolve overlay has class "bg-black" and no backgroundImage.
  </action>
  <verify>
    <automated>cd C:/dev/good-morning-nikhil && npx vitest run test/scene-transition.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File test/scene-transition.test.tsx exists
    - Contains at least 6 test cases in a describe("SceneTransition") block
    - Test names include "VIS-01" and "VIS-02" labels
    - grep "tv-static" test/scene-transition.test.tsx returns matches (proves static detection)
    - grep "does NOT fire" test/scene-transition.test.tsx returns at least 3 matches (within-show tests)
    - npx vitest run test/scene-transition.test.tsx exits 0 with all tests passing
    - npm test still passes all 62+ existing tests (no regressions)
  </acceptance_criteria>
  <done>6 tests pass proving SceneTransition fires static on show changes (VIS-01), does NOT fire on within-show transitions (VIS-02), handles first screen and dissolve cases. All existing tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→component | Tests render SceneTransition with various props |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Info Disclosure) | SceneTransition | accept | Component is visual-only, no data processing, no user input |
</threat_model>

<verification>
- `npx vitest run test/scene-transition.test.tsx` passes all 6 tests
- `npm test` passes all tests (62 existing + 6 new = 68+)
- VIS-01 proven: test shows static fires when previousShow !== screen.show
- VIS-02 proven: tests show no static when previousShow === screen.show
</verification>

<success_criteria>
- SceneTransition has automated test coverage for show-change and within-show transitions
- VIS-02 requirement can be marked Complete with test evidence
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-ambient-motion-integration/03-01-SUMMARY.md`
</output>
