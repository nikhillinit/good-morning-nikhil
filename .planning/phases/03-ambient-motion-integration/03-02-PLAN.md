---
phase: 03-ambient-motion-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ambient/PaperShimmer.tsx
  - src/components/ambient/DustMotes.tsx
  - src/components/ambient/HazeDrift.tsx
  - src/components/ambient/FirelightVariation.tsx
  - src/components/ambient/CandleFlicker.tsx
  - src/components/ambient/SpotlightPulse.tsx
  - src/components/ambient/StageLightShimmer.tsx
autonomous: false
requirements:
  - VIS-03
  - VIS-04

must_haves:
  truths:
    - "Paper shimmer opacity oscillates between 2% and 5% (visible but subtle)"
    - "DustMotes use warm-tinted color matching studio lighting, not pure white"
    - "HazeDrift moves slowly enough to feel atmospheric, not distracting"
    - "FirelightVariation gradient originates from the left (matching firepit position in tribal-council stills)"
    - "All ambient layers remain pointer-events-none and z-[5]"
    - "TypeScript compiles clean after parameter changes"
  artifacts:
    - path: "src/components/ambient/PaperShimmer.tsx"
      provides: "Paper shimmer with 2-5% opacity range"
      contains: "opacity: [0.02, 0.05]"
    - path: "src/components/ambient/DustMotes.tsx"
      provides: "Dust motes with warm-tinted color"
      contains: "bg-amber"
    - path: "src/components/ambient/HazeDrift.tsx"
      provides: "Haze drift calibrated to 30s+ duration"
      contains: "duration: 30"
    - path: "src/components/ambient/FirelightVariation.tsx"
      provides: "Firelight gradient from left side"
      contains: "rgba(255,200,140"
  key_links:
    - from: "src/components/ambient/PaperShimmer.tsx"
      to: "src/components/ScreenPlayer.tsx"
      via: "direct import on every screen"
      pattern: "import.*PaperShimmer"
    - from: "src/lib/ambient-map.tsx"
      to: "src/components/ambient/index.ts"
      via: "barrel imports for per-scene layers"
      pattern: "from.*@/components/ambient"
---

<objective>
Tune ambient motion parameters for visual quality against real stills (Task 3B from roadmap). Adjust paper shimmer to 2-5% opacity, warm-tint dust mote color, calibrate haze drift speed, and position firelight gradient correctly.

Purpose: The ambient layers were built with placeholder values. This plan tunes them to look correct against the actual backgrounds -- paper shimmer should be barely visible, dust should feel warm, haze should drift slowly, and firelight should match the campfire position in the tribal council scene.

Output: Updated ambient component files with tuned parameters, verified visually.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ambient/PaperShimmer.tsx
@src/components/ambient/DustMotes.tsx
@src/components/ambient/HazeDrift.tsx
@src/components/ambient/FirelightVariation.tsx
@src/components/ambient/CandleFlicker.tsx
@src/components/ambient/SpotlightPulse.tsx
@src/components/ambient/StageLightShimmer.tsx
@src/lib/ambient-map.tsx
@src/components/ScreenPlayer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune all ambient layer parameters</name>
  <files>
    src/components/ambient/PaperShimmer.tsx
    src/components/ambient/DustMotes.tsx
    src/components/ambient/HazeDrift.tsx
    src/components/ambient/FirelightVariation.tsx
    src/components/ambient/CandleFlicker.tsx
    src/components/ambient/SpotlightPulse.tsx
    src/components/ambient/StageLightShimmer.tsx
  </files>
  <read_first>
    - src/components/ambient/PaperShimmer.tsx
    - src/components/ambient/DustMotes.tsx
    - src/components/ambient/HazeDrift.tsx
    - src/components/ambient/FirelightVariation.tsx
    - src/components/ambient/CandleFlicker.tsx
    - src/components/ambient/SpotlightPulse.tsx
    - src/components/ambient/StageLightShimmer.tsx
    - src/lib/animations.ts (for EASE_BREATHE, EASE_ORGANIC constants)
  </read_first>
  <action>
Apply the following parameter changes to each ambient component. All changes are CSS/Framer Motion values only -- no structural changes.

**PaperShimmer.tsx** (VIS-04 tuning: "visible but barely"):
- Change `animate={{ opacity: [0.03, 0.08] }}` to `animate={{ opacity: [0.02, 0.05] }}`
  - Current 3-8% is too visible. Target 2-5% per roadmap spec.
- Keep duration 6s, EASE_BREATHE, mix-blend-overlay, backgroundSize 256px -- all good.

**DustMotes.tsx** (dust mote color tuning):
- Change `className="absolute rounded-full bg-white/20 pointer-events-none"` in the Mote component to `className="absolute rounded-full bg-amber-200/20 pointer-events-none"`
  - Pure white dust looks cold. bg-amber-200/20 gives a warm studio-light tint matching the Feud set's warm lighting.
- Change closing motes similarly (same Mote component handles both).
- Change opacity animate from `[0.15, 0.35]` to `[0.12, 0.28]` -- slightly subtler drift.
- Keep x movement 120px over 15s, sizes, and positions unchanged.

**HazeDrift.tsx** (drift speed calibration):
- Change `transition={{ duration: 20, ease: "linear", repeat: Infinity }}` to `transition={{ duration: 30, ease: "linear", repeat: Infinity }}`
  - 20s is too fast. 30s gives a lazy warehouse haze feel appropriate for Shark Tank.
- Change opacity from `0.06` to `0.05` -- slightly more subtle.
- Keep 45% height, 200% width, repeat-x pattern unchanged.

**FirelightVariation.tsx** (firelight warmth and position):
- Change gradient from `rgba(255,240,210,0.08)` to `rgba(255,200,140,0.08)` at 0% stop
  - More orange/amber to match campfire warmth, less pale yellow.
- Change mid gradient from `rgba(255,240,210,0.03)` to `rgba(255,200,140,0.03)` at 35% stop
  - Consistent warmer tone through gradient.
- Keep 90deg direction (left-to-right), opacity [0.75, 1.0], 5s duration, EASE_ORGANIC.

**CandleFlicker.tsx** (candle warmth tuning):
- Change glow color from `rgba(255,248,230,0.10)` to `rgba(255,235,200,0.12)` in SconceGlow
  - Slightly warmer and marginally more visible for Bachelor mansion's romantic mood.
- Keep positions (12%/15% and 78%/15%), sizes, timing unchanged.

**SpotlightPulse.tsx** (spotlight centering):
- Change `left: "42%"` to `left: "40%"` -- center the spotlight more precisely on the pedestal.
- Change glow color from `rgba(255,252,240,0.12)` to `rgba(255,250,235,0.14)` -- slightly warmer and brighter for sponsor segment.
- Keep size 16%x25%, opacity [0.92, 1.0], 8s EASE_BREATHE.

**StageLightShimmer.tsx** (stage light tuning):
- Change glow color from `rgba(255,255,255,0.04)` to `rgba(255,255,255,0.05)` -- very slightly more visible to read on Maury's lighter set.
- Change opacity from `[0.95, 1.0]` to `[0.90, 1.0]` -- wider breathe range for more visible shimmer.
- Keep position (25%/10%), size (50%x30%), 10s EASE_BREATHE.

After all edits, run `npx tsc --noEmit` to confirm TypeScript compiles clean.
  </action>
  <verify>
    <automated>cd C:/dev/good-morning-nikhil && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep "opacity: \[0.02, 0.05\]" src/components/ambient/PaperShimmer.tsx returns a match
    - grep "bg-amber-200" src/components/ambient/DustMotes.tsx returns a match
    - grep "duration: 30" src/components/ambient/HazeDrift.tsx returns a match
    - grep "rgba(255,200,140" src/components/ambient/FirelightVariation.tsx returns a match
    - grep "rgba(255,235,200" src/components/ambient/CandleFlicker.tsx returns a match
    - grep 'left: "40%"' src/components/ambient/SpotlightPulse.tsx returns a match
    - grep "0.05" src/components/ambient/StageLightShimmer.tsx returns a match
    - npx tsc --noEmit exits 0
    - npm test passes all tests (no regressions)
  </acceptance_criteria>
  <done>All 7 ambient components have tuned parameters: PaperShimmer 2-5%, warm dust motes, slower haze, warmer firelight, warmer candle glow, centered spotlight, visible stage shimmer. TypeScript compiles clean, all tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of ambient layers on each show segment</name>
  <files>none (visual verification only)</files>
  <action>
Start the dev server with `npm run dev`. Open http://localhost:3000 in Chrome DevTools mobile viewport (375px width). Click through all screens to visually verify each ambient layer renders correctly against the real stills. Present the verification checklist from how-to-verify to the user for their sign-off.
  </action>
  <what-built>Tuned all 7 ambient motion layers: PaperShimmer opacity reduced to 2-5%, DustMotes warm-tinted with bg-amber-200, HazeDrift slowed to 30s, FirelightVariation gradient warmed to orange-amber, CandleFlicker glow warmed, SpotlightPulse centered, StageLightShimmer slightly brighter.</what-built>
  <how-to-verify>
    1. Run `npm run dev` to start the dev server
    2. Open http://localhost:3000 on your phone or Chrome DevTools mobile viewport (375px width)
    3. Click through each screen and verify ambient layers:
       - Screen 0 (Cold Open / feud-board): Look for subtle warm-tinted dust motes drifting right-to-left. Paper shimmer should be barely visible grain texture.
       - Screen 3A-3C (Family Feud): Same DustMotes, verify they look warm not cold-white.
       - Screen 4A-4B (Sponsor): Centered spotlight glow on pedestal area.
       - Screen 5A-5B (Bachelor): Two candle-like glows at upper-left and upper-right corners.
       - Screen 5C (Limo): Same candle glow, dimmer background.
       - Screen 6A-6B (Shark Tank): Slow haze wisp drifting horizontally in upper half. Should be barely perceptible, not distracting.
       - Screen 7 (Survivor): Orange-amber firelight glow from left side, matching campfire position.
       - Screen 8 (Maury): Subtle stage light shimmer in upper-center area.
       - Screen 9 (Control Room): Dust motes (same as Feud).
       - Screen 10 (Credits): Larger/more dust motes for closing feel.
    4. On EVERY screen, confirm paper shimmer is visible but very subtle (look for faint grain texture pulsing).
    5. Verify no ambient layer is distracting or pulls attention from captions/UI.
  </how-to-verify>
  <verify>
    <automated>cd C:/dev/good-morning-nikhil && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" confirming all ambient layers look correct
    - No ambient layer reported as too prominent or distracting
    - Paper shimmer confirmed visible on every screen
  </acceptance_criteria>
  <done>User has visually verified all ambient layers look correct on real stills. All 7 layers approved for production.</done>
  <resume-signal>Type "approved" if all ambient layers look good, or describe which scenes need adjustment (e.g., "firelight too bright on Survivor", "haze invisible on Shark Tank").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Visual-only CSS/animation changes, no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | D (Denial of Service) | Ambient animations | accept | Framer Motion animations are GPU-accelerated, 7 lightweight layers is well within mobile performance budget. Verified in Phase 4 perf check. |
</threat_model>

<verification>
- TypeScript compiles with zero errors
- All existing tests pass (no regressions)
- Visual checkpoint confirms ambient layers look correct on real stills
- Paper shimmer visible but subtle on every screen
</verification>

<success_criteria>
- All 7 ambient components have tuned production-ready parameters
- Human has visually verified ambient layers on each show segment
- No ambient layer is distracting or too prominent
- Paper shimmer visible (barely) on every screen per VIS-04
</success_criteria>

<output>
After completion, create `.planning/phases/03-ambient-motion-integration/03-02-SUMMARY.md`
</output>
