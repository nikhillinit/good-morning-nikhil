---
phase: quick
plan: 260413-uj1
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - package-lock.json
  - playwright.config.ts
  - test/e2e/core-journey.spec.ts
  - docs/user-journeys.md
  - TESTING.md
autonomous: true
requirements: [QUICK-260413-uj1]

must_haves:
  truths:
    - "A real browser E2E journey exists for the first-run respondent flow"
    - "The browser journey reflects the implemented survey flow in src/app/page.tsx, not an imagined multi-page app"
    - "The journey covers consent gate, intro advancement, relationship selection, first answer submission, and handoff to the next screen"
    - "Journey documentation and testing guidance point to the new browser lane"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Browser E2E harness configuration"
    - path: "test/e2e/core-journey.spec.ts"
      provides: "Core respondent browser journey from consent to first answer handoff"
    - path: ".gitignore"
      provides: "Playwright artifact path ignored to avoid git noise"
    - path: "docs/user-journeys.md"
      provides: "Behavioral specification updated with browser journey coverage"
    - path: "TESTING.md"
      provides: "Runnable E2E command surface and pointer to the journey spec"
  key_links:
    - from: "test/e2e/core-journey.spec.ts"
      to: "src/app/page.tsx"
      via: "SurveyFlow first-run orchestration"
      pattern: "SurveyFlow"
    - from: "test/e2e/core-journey.spec.ts"
      to: "test/"
      via: "Builds on existing unit/integration coverage"
      pattern: "media-gate.test"
---

<objective>
Add one real browser-level E2E journey for Good Morning, Nikhil and align the
docs/testing references around it.

Purpose: The app is a single animated survey flow whose highest-value missing
coverage was the browser seam from consent gate into the first real answer.
This task adds that seam in Playwright, keeps the scope bounded to one stable
journey, and updates the docs so future browser work extends a clear source of
truth.

Output: Playwright config + one core spec, doc pointers, verification results,
and a quick-task summary.
</objective>

<execution_context>
@CLAUDE.md
@TESTING.md
@SYNTHETIC-USER-TEST.md
@src/app/page.tsx
@src/components/ScreenPlayer.tsx
@src/components/ui-inputs/index.tsx
@src/data/screens.ts
@test/flow.test.ts
@test/media-gate.test.tsx
@test/question-prompt.test.tsx
@test/review-screen.test.tsx
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add Playwright harness and one core browser journey</name>
  <files>package.json, package-lock.json, playwright.config.ts, test/e2e/core-journey.spec.ts</files>
  <behavior>
    - Add a stable browser harness using Playwright
    - Cover the first-run respondent flow through the first successful answer and next-screen handoff
    - Avoid microphone dependence by using the typed fallback on voice-first screens
    - Keep selectors accessibility-first and scoped to the live UI
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Align docs with the new browser lane</name>
  <files>docs/user-journeys.md, TESTING.md</files>
  <behavior>
    - Add the new E2E journey to the automated coverage map
    - Update testing guidance so the browser command surface is explicit
  </behavior>
</task>

</tasks>

<verification>
1. `npm test`
2. `npm run lint`
3. `npx tsc --noEmit`
</verification>

<success_criteria>
- A real browser test proves the first-run respondent path works
- Journey documentation is tied to the new browser lane and existing test layers
- The testing guide exposes the E2E command surface clearly
</success_criteria>
