---
phase: quick
plan: 260413-1139
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui-inputs/index.tsx
  - test/ui-inputs-contract.test.tsx
autonomous: true
requirements: [QUICK-260413-1139]

must_haves:
  truths:
    - "Answer screens follow a more consistent completion contract across text and selection inputs"
    - "Respondents can explicitly skip rigid screens that previously forced completion"
    - "The changes are protected by focused UI contract tests"
  artifacts:
    - path: "src/components/ui-inputs/index.tsx"
      provides: "Normalized respondent-facing completion contract for answer inputs"
    - path: "test/ui-inputs-contract.test.tsx"
      provides: "Regression coverage for skip and completion behavior across input types"
---

<objective>
Address the first slice from the parallel analysis: normalize the respondent-facing
screen contract so the survey feels like one system instead of a mix of interaction
rules.

Purpose: The current prototype still has rigid outlier screens (`TwoText`,
`SingleSelect`, `MultiSelect`, and `ThreeText`) that do not match the clearer
skip/confirm model already used elsewhere. This task reduces that inconsistency
without widening into copy or export work.
</objective>

<execution_context>
@docs/user-journeys.md
@docs/screen-experience-brief.md
@src/components/ui-inputs/index.tsx
@test/ui-inputs-hydration.test.tsx
@test/question-prompt.test.tsx
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Normalize answer-screen completion rules</name>
  <files>src/components/ui-inputs/index.tsx</files>
  <behavior>
    - Add explicit skip affordances to the currently rigid answer types
    - Make the minimum requirement on `ThreeText` more legible
    - Preserve the existing explicit confirm step (`Lock it in`) instead of instant advancement
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Lock the new contract with focused tests</name>
  <files>test/ui-inputs-contract.test.tsx</files>
  <behavior>
    - Verify skip affordances exist on the previously rigid answer types
    - Verify skip submits `null`
    - Verify `ThreeText` still requires at least one answer before submit
  </behavior>
</task>
</tasks>

<verification>
1. `npm test`
2. `npm run lint`
3. `npx tsc --noEmit`
</verification>

<success_criteria>
- The outlier answer screens no longer feel trapped compared to the rest of the survey
- Contract changes are small, reversible, and test-backed
</success_criteria>
