---
phase: quick
plan: 260413-1200
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/screens.ts
  - src/app/globals.css
  - src/components/ScreenPlayer.tsx
  - src/components/Captions.tsx
  - src/components/SceneTransition.tsx
  - src/components/ui-inputs/index.tsx
autonomous: true
requirements: [QUICK-260413-1200]

must_haves:
  truths:
    - "The cold open no longer blocks interaction for nearly a minute"
    - "Each show segment reads more distinctly through accent theming"
    - "Show-change transitions feel more like channel changes than generic fades"
    - "Captions read like broadcast lower-thirds instead of generic text boxes"
  artifacts:
    - path: "src/data/screens.ts"
      provides: "Pacing-adjusted cold-open timing"
    - path: "src/app/globals.css"
      provides: "Show-specific theme tokens and lower-third styles"
    - path: "src/components/ScreenPlayer.tsx"
      provides: "Theme hooks, stronger background visibility, and immediate skip for long VO"
    - path: "src/components/Captions.tsx"
      provides: "Broadcast-style lower-third captions"
    - path: "src/components/SceneTransition.tsx"
      provides: "More forceful show-change transition treatment"
---

<objective>
Improve the most visible pacing and TV-show identity problems without widening
into a full art-direction rewrite.

Purpose: The app already works, but it still feels like a dark form with themed
assets rather than a coherent show. This pass strengthens the first impression,
makes the sets/backgrounds more legible, and gives show changes more energy.
</objective>

<execution_context>
@docs/screen-experience-brief.md
@docs/user-journeys.md
@src/data/screens.ts
@src/app/globals.css
@src/components/ScreenPlayer.tsx
@src/components/Captions.tsx
@src/components/SceneTransition.tsx
@test/scene-transition.test.tsx
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Improve first-run pacing</name>
  <files>src/data/screens.ts, src/components/ScreenPlayer.tsx</files>
  <behavior>
    - Reduce cold-open wait time before the start CTA appears
    - Make skip affordance available immediately on long-VO screens
    - Keep the implementation bounded to the current flow
  </behavior>
</task>

<task type="auto">
  <name>Task 2: Add show-specific visual identity</name>
  <files>src/app/globals.css, src/components/ScreenPlayer.tsx, src/components/ui-inputs/index.tsx</files>
  <behavior>
    - Introduce per-show accent tokens via data attributes / CSS variables
    - Improve background visibility by reducing the “dark form on top of image” effect
  </behavior>
</task>

<task type="auto">
  <name>Task 3: Strengthen captions and transitions</name>
  <files>src/components/Captions.tsx, src/components/SceneTransition.tsx</files>
  <behavior>
    - Make captions feel like lower-thirds
    - Make show-to-show transitions read more like a channel change
  </behavior>
</task>
</tasks>

<verification>
1. `npm test`
2. `npm run lint`
3. `npx tsc --noEmit`
4. `npm run test:e2e`
</verification>

<success_criteria>
- The app feels faster and more like television before any copy rewrite
- Changes are visible in real use without destabilizing the flow
</success_criteria>
