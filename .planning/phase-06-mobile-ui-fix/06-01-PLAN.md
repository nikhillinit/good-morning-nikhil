---
phase: 06-mobile-ui-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/QuestionPrompt.tsx
  - src/components/ui-inputs/index.tsx
  - src/components/ShowBadge.tsx
  - src/components/MuteToggle.tsx
  - src/components/SkipButton.tsx
autonomous: true
requirements:
  - P0-1
  - P0-3
  - P0-4
  - P0-5
  - P1-2
  - P1-3
  - P1-4
  - P1-5
  - P1-6
  - P1-7
  - P1-8
  - P1-9
  - P1-10
  - P2-2
  - P2-3

must_haves:
  truths:
    - "globals.css contains --card, --card-border, --prompt, --input-border, --chip-idle-border, --nav-control tokens — all consumed by components"
    - "QuestionPrompt uses text-[--prompt] text-base (not text-sm text-zinc-400)"
    - "inputField constant uses border-[--input-border] and bg-zinc-900/90"
    - "chipIdle constant uses border-[--chip-idle-border] (not border-zinc-700)"
    - "MuteToggle is h-12 w-12 with text-white and bg-[--nav-control]"
    - "SkipButton uses bg-[--nav-control] and is positioned at top-16 right-4"
    - "ShowBadge uses bg-[--nav-control] (not bg-black/60)"
    - "All text upgrades applied: MultiSelect label zinc-200, InvestOrPass text-sm zinc-300, MadLib stem text-white, LongTextWithAudio quote zinc-200, skipBtn zinc-400, ContinueButton desc zinc-200, TwoText label zinc-300, RelationshipPicker label zinc-300"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS custom property tokens for content card, inputs, nav controls"
    - path: "src/components/QuestionPrompt.tsx"
      provides: "Contrast-safe question prompt (white text, base size)"
    - path: "src/components/ui-inputs/index.tsx"
      provides: "Updated style constants and inline text colors for all input components"
    - path: "src/components/ShowBadge.tsx"
      provides: "Dark glass nav control at 70% opacity"
    - path: "src/components/MuteToggle.tsx"
      provides: "48px touch target, full white text, 70% dark glass"
    - path: "src/components/SkipButton.tsx"
      provides: "Dark glass treatment, repositioned below MuteToggle"
---

<objective>
Apply all style token and component contrast fixes from the Phase 6 UI-SPEC. This covers P0 (critical contrast), P1 (interaction clarity), and P2 (label) items that don't require structural layout changes.

Purpose: The mobile UI audit found text-on-overlay contrast failures across every screen. This plan fixes them by adding CSS tokens and upgrading Tailwind classes in 6 files. No structural/layout changes — those are in Plan 06-02.

Output: All 6 files updated with passing TypeScript compilation and existing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-06-mobile-ui-fix/06-UI-SPEC.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/components/QuestionPrompt.tsx
@src/components/ui-inputs/index.tsx
@src/components/ShowBadge.tsx
@src/components/MuteToggle.tsx
@src/components/SkipButton.tsx
</context>

<tasks>

## Task 1: Add CSS design tokens to globals.css

**File:** `src/app/globals.css`

Add these tokens inside the existing `:root` block, after the `--error` line:

```css
--card: #1c1c1f;
--card-border: rgba(255,255,255,0.08);
--prompt: #ffffff;
--input-border: rgba(255,255,255,0.18);
--chip-idle-border: rgba(255,255,255,0.20);
--nav-control: rgba(0,0,0,0.70);
```

All 6 tokens are consumed by component classes in this plan or Plan 06-02.
Removed from UI-SPEC reference list (not needed as CSS): `--input-border-focus` (existing `--accent` serves this), `--placeholder` (Tailwind zinc-500 utility sufficient), `--chip-idle-text` (Tailwind zinc-300 utility sufficient).

**Verification:** `grep --card globals.css` returns match.

## Task 2: Fix QuestionPrompt contrast

**File:** `src/components/QuestionPrompt.tsx`

Change line 22 className from:
```
text-sm italic text-zinc-400
```
to:
```
text-base italic text-[--prompt]
```

Consumes `--prompt` token (#ffffff). Keep all other classes (`mb-4 max-w-md px-2 text-center`).

**Verification:** grep for `text-\[--prompt\]` in QuestionPrompt.tsx.

## Task 3: Update ui-inputs style constants

**File:** `src/components/ui-inputs/index.tsx`

### 3a. inputField constant (line ~21)
Change `border-zinc-700` to `border-[--input-border]`
Change `bg-zinc-900/80` to `bg-zinc-900/90`

### 3b. skipBtn constant (line ~24)
Change `text-zinc-500 hover:text-zinc-300` to `text-zinc-400 hover:text-zinc-200`

### 3c. chipIdle constant (line ~33)
Change `border-zinc-700` to `border-[--chip-idle-border]`

### 3d. MultiSelect label (line ~255)
Change `text-sm text-zinc-400` to `text-sm text-zinc-200`

### 3e. SingleSelect label (line ~299)
Change `text-sm text-zinc-400` to `text-sm text-zinc-200`

### 3f. InvestOrPass helper (line ~344)
Change `text-xs text-zinc-500` to `text-sm text-zinc-300`

### 3g. MadLib instruction (line ~395)
Change `text-xs text-zinc-500` to `text-sm text-zinc-300`

### 3h. MadLib stem text (line ~396)
Change `text-base text-zinc-300` to `text-base text-white`

### 3i. LongTextWithAudio quote (line ~444)
Change `text-base italic text-zinc-400` to `text-base italic text-zinc-200`

### 3j. ContinueButton description (line ~52)
Change `text-sm text-zinc-400` to `text-sm text-zinc-200`

### 3k. TwoText label (line ~500)
Change `text-sm text-zinc-400` to `text-sm text-zinc-300`

### 3l. RelationshipPicker checkbox label (line ~574)
Change `text-sm text-zinc-400` to `text-sm text-zinc-300`

**Verification:** grep for `text-zinc-400` in index.tsx — should return 0 matches (all upgraded).

## Task 4: Update ShowBadge dark glass

**File:** `src/components/ShowBadge.tsx`

Change `bg-black/60` to `bg-[--nav-control]` in the className (line ~15).

**Verification:** grep for `bg-\[--nav-control\]` in ShowBadge.tsx.

## Task 5: Update MuteToggle size + contrast

**File:** `src/components/MuteToggle.tsx`

Change className (line ~13):
- `h-10 w-10` → `h-12 w-12`
- `bg-black/60` → `bg-[--nav-control]`
- `text-white/70` → `text-white`

**Verification:** grep for `h-12 w-12` and `bg-\[--nav-control\]` in MuteToggle.tsx.

## Task 6: Update SkipButton dark glass + position

**File:** `src/components/SkipButton.tsx`

Change className (line ~19):
- `bg-white/10` → `bg-[--nav-control]`
- `right-4 top-4` → `right-4 top-16` (below MuteToggle)
- Remove `backdrop-blur-sm` (redundant with opaque dark glass)
- Add `border border-white/10`

Also update whileHover (line ~18):
- `backgroundColor: "rgba(255,255,255,0.2)"` → `backgroundColor: "rgba(0,0,0,0.8)"`

**Verification:** grep for `bg-\[--nav-control\]` and `top-16` in SkipButton.tsx.

</tasks>

<verification>
After all tasks:
1. `npx tsc --noEmit` — zero errors
2. `npm test` — all existing tests pass
3. grep `text-zinc-400` in ui-inputs/index.tsx — should be 0 matches (all upgraded)
4. grep `bg-black/60` across ShowBadge, MuteToggle, SkipButton — should be 0 matches (all use --nav-control)
5. grep `--nav-control` in globals.css — confirms token exists
6. grep `bg-\[--nav-control\]` across ShowBadge, MuteToggle, SkipButton — confirms token consumed
</verification>
